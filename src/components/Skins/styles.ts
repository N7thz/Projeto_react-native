import { StyleSheet, Platform } from "react-native"

export const styles = StyleSheet.create({
  image: {
    width: 300,
    height: 200,
    margin: 2.5,
    resizeMode: 'cover',
    borderRadius: 8,
  },
  container: {
      marginBottom: 20,
  },
 
})